import React, { useState } from 'react';
import Fieldset from "../Fieldset";
import { currencies } from './currencies';
import "./style.css"

const Form = () => (
    <form className="form">
                <Fieldset />
            </form>
);

export default Form;